<!DOCTYPE html>
<html lang="fr">

<head>
    <title><%= content_for(:title) ? "#{content_for(:title)} | Irawo" : "Irawo" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://cdn.tailwindcss.com"></script>

    <%= yield :head %>

    <%= stylesheet_link_tag 'main', media: 'all', 'data-turbo-track': 'reload' %>
</head>

<body>
    <div>
        <header style="z-index:10; margin-bottom:75px">
            <nav class="fixed top-0 left-0 right-0 z-50" style="box-shadow: 0 1.2rem 3.2rem rgba(0, 0, 0, 0.02); background-color:rgba(255, 255, 255, 0.97);">
                <div class="flex items-center justify-between w-full first-child-nav py-4 px-10 container mx-auto">
                    <div class="flex items-center justify-between gap-12 w-full">
                        <h2 class="font-bold text-3xl">
                            Logo
                        </h2>
                        <a href="<%= mon_espace_path %>" class="border-2 rounded-md p-2 border-[#FBC029] hover:bg-[#FBC029] hover:text-white cursor-pointer transition-all duration-300">
                            Mon espace
                        </a>
                    </div>
                </div>
            </nav>
        </header>
        <main>
            <%= yield %>
        </main>
        <footer class="py-10 bg-gray-900 text-white">
            <div class="container mx-auto flex flex-col items-center gap-4">
                <div class="flex items-center justify-between gap-2">
                    <a href="#" class="text-sm">Mentions légales</a>
                    <a href="#" class="text-sm">Contact</a>
                </div>
                <p class="text-sm">Tous droits réservés © 2024</p>
            </div>
        </footer>
    </div>
</body>
</html>